<h3 style="padding-top: 10px;">Search for quotes by category</h3>
<%= form_tag search_path, method: :get do %>
    <div class="form-group" style="padding-bottom: 10px;">
    <%= text_field_tag :category_query, params[:category_query], placeholder: 'Enter an category', class: "form-control" %>
    </div>
    <%= submit_tag 'Search Quotes', class: "btn btn-primary" %>
  <% end %>
  <% if params[:category_query].present? %>
    <% if @quotematch.present? %>
        <h4 style="padding-top: 15px;">Results:</h4>
            <% @quotematch.each do |quote| %>
            <h5 class="text-primary"><%= [quote.author.fname, quote.author.lname].compact.join(' ') %></h5>
            <p><%= quote.quote %> [<a href="/quotes/<%= quote.id %>">Read full quote</a>]</p>
            <br />
            <% end %>
    <% else %>
        <p>No matching quotes found.</p>
    <% end %>
<% end %>